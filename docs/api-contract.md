# API Contract

REST + OpenAPI.

Initial resources:

Projects
Documents
Revisions

Frontend API types should be generated from OpenAPI where possible.
Backend validates data integrity but never performs graph layout.
