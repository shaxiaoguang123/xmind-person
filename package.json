{
  "name": "xmind-person",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "lint": "npm run lint --workspace @xmind-person/web",
    "typecheck": "npm run typecheck --workspace @xmind-person/web",
    "test": "npm run test --workspace @xmind-person/web",
    "build": "npm run build --workspace @xmind-person/web"
  }
}
